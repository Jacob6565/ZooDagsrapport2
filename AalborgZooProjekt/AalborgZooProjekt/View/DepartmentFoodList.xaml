<UserControl x:Class="AalborgZooProjekt.View.DepartmentFoodList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AalborgZooProjekt.View"
             mc:Ignorable="d" 
             DataContext="{Binding Tester, Source={StaticResource Locator}}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Label x:Name="Test"/>
        <DataGrid ItemsSource="{Binding DummyFoodList}" IsReadOnly="True" Name="dgFoodList">
        </DataGrid>
    </Grid>

</UserControl>
